services:
  mesh-controller:
    build: .
    image: mesh-controller-ihost:6.2.0
    network_mode: host
    restart: unless-stopped
    volumes:
      - mesh-controller-data:/data
volumes:
  mesh-controller-data:
